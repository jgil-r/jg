<header>
    <ul>
        <li>
            <a class="header-button" href="https://github.com/jgil-r" target="_blank" rel="noopener noreferrer">Github</a>
        </li>
        <li>
            <a class="header-button" href="https://twitter.com/jgil_r" target="_blank" rel="noopener noreferrer">Twitter</a>
        </li>
    </ul>
</header>

<style>
    header {
        height: 64px;
        display: flex;
        justify-content: flex-end;
        align-items: center;
        margin: 3rem 0 5rem;
        position: sticky;
        top: 0;
        background-color: var(--color-secondary);
    }

    ul {
        padding-left: 0;
        list-style: none;
        display: flex;
    }

    ul > li {
        margin-left: 1rem;
    }

    .header-button {
        padding: var(--space-sm) var(--space-md);
        border: 1px solid var(--color-primary);
        border-radius: var(--radius-sm);
        color: var(--color-primary);
        background-color: var(--color-secondary);
    }

    .header-button:hover {
        color: var(--color-secondary);
        background-color: var(--color-primary);
    }

    @media (prefers-color-scheme: dark) {
        header {
            background-color: var(--color-primary);
        }
        .header-button {
            border: 1px solid var(--color-secondary);
            color: var(--color-secondary);
            background-color: var(--color-primary);
        }

        .header-button:hover {
            color: var(--color-primary);
            background-color: var(--color-secondary);
        }
    }
</style>
